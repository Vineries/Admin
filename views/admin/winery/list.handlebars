<a href="winery/new">New</a>
<table class="ui sortable celled table">
    <thead>
        <tr>
            <th class="sorted ascending">Name</th>
            <th>Description</th>
            <th>Category</th>
        </tr>
    </thead>
    <tbody>
        {{#each winery_data}}
          <tr>
              <td><a href="/admin/winery/{{ _id }}">{{name}}</a></td>
              <td>{{website}}</td>
              <td>{{phone}}</td>
          </tr>
        
        {{/each}}
    <tfoot>
        <tr>
            <th colspan='3'>Wineries: {{wines.length}}</th>
        </tr>
    </tfoot>
</table>

{{#section 'actionbar'}}
<div class="ui vertical menu">
  <a class="active teal item" href="/admin/winery/new">
    New
  </a>
  <a class="item">
    Spam
    <div class="ui label">51</div>
  </a>
  <a class="item">
    Updates
    <div class="ui label">1</div>
  </a>
  <div class="item">
    <div class="ui transparent icon input">
      <input type="text" placeholder="Search mail...">
      <i class="search icon"></i>
    </div>
  </div>
</div>
{{/section}}